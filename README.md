# extsort — FileOrganizer

`input/` 폴더의 파일을 `config.json` 규칙에 따라 자동으로 분류하여 `output/` 폴더로 이동시키는 파이썬 스크립트입니다.

---

## 📁 프로젝트 구조

```
extsort/
├── config.json      # 분류 규칙 정의
├── organize.py      # 메인 스크립트
├── input/           # 정리할 파일을 여기에 넣으세요
├── output/          # 분류된 파일이 저장되는 폴더
└── README.md
```

---

## 🚀 사용법

### 1. 파일 준비
```
input/ 폴더에 정리할 파일들을 넣습니다.
```

### 2. 스크립트 실행
```bash
python organize.py
```

### 3. 결과 확인
```
output/
├── images/       ← .jpg, .png, .gif 등
├── documents/    ← .pdf, .docx, .txt 등
├── videos/       ← .mp4, .avi 등
├── audio/        ← .mp3, .wav 등
├── archives/     ← .zip, .rar 등
├── code/         ← .py, .js, .java 등
├── logs/         ← .log
├── temp/         ← tmp_, temp_ 접두사
├── backup/       ← bak_, backup_, old_ 접두사
├── reports/      ← report_, rpt_ 접두사
└── others/       ← 규칙에 맞지 않는 파일
```

---

## ⚙️ config.json 규칙 설명

### 규칙 타입

| 타입        | 설명                          | 예시                         |
|-------------|-------------------------------|------------------------------|
| `extension` | 파일 확장자로 분류            | `.jpg`, `.pdf`, `.mp3`       |
| `prefix`    | 파일명 접두사(prefix)로 분류  | `tmp_`, `report_`, `bak_`    |

### 규칙 구조
```json
{
  "rules": [
    {
      "name": "Images",
      "type": "extension",
      "patterns": [".jpg", ".png"],
      "output_folder": "images"
    },
    {
      "name": "Temp Files",
      "type": "prefix",
      "patterns": ["tmp_", "temp_"],
      "output_folder": "temp"
    }
  ],
  "settings": {
    "unmatched_folder": "others",
    "overwrite": false,
    "dry_run": false
  }
}
```

### settings 옵션

| 옵션               | 기본값     | 설명                                                    |
|--------------------|------------|---------------------------------------------------------|
| `unmatched_folder` | `"others"` | 규칙에 맞지 않는 파일이 이동될 폴더명                  |
| `overwrite`        | `false`    | `true` 시 동일 파일명 덮어쓰기, `false` 시 타임스탬프 추가 |
| `dry_run`          | `false`    | `true` 시 실제 이동 없이 결과만 미리 확인              |

---

## 📊 출력 예시

```
[INFO] 5개 파일 처리 시작...

  ✔  photo.jpg        →  images/
  ✔  report_2024.pdf  →  reports/
  ✔  tmp_cache.dat    →  temp/
  ✔  music.mp3        →  audio/
  ✔  unknown.xyz      →  others/

──────────────────────────────────────────────────
  📊  FileOrganizer 결과 리포트
──────────────────────────────────────────────────
  폴더별 이동 현황:
    images          █  1개
    reports         █  1개
    temp            █  1개
    audio           █  1개
    others          █  1개
──────────────────────────────────────────────────
  총 처리 파일  : 5개
  성공         : 5개
  실패         : 0개
  소요 시간    : 0.003초
──────────────────────────────────────────────────
```

---

## 📋 지원 형식

| 카테고리  | 확장자                                              |
|-----------|-----------------------------------------------------|
| 이미지    | jpg, jpeg, png, gif, bmp, svg, webp, ico            |
| 문서      | pdf, doc, docx, txt, md, xlsx, xls, pptx, ppt, csv |
| 동영상    | mp4, avi, mov, mkv, wmv, flv, webm                  |
| 오디오    | mp3, wav, flac, aac, ogg, wma, m4a                  |
| 압축      | zip, rar, 7z, tar, gz, bz2                          |
| 코드      | py, js, ts, java, c, cpp, cs, go, rs, html, css 등  |
| 로그      | log                                                 |

---

## 📦 요구사항

- Python 3.9+
- 외부 라이브러리 없음 (표준 라이브러리만 사용)
